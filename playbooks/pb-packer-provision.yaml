- name: Setup templated Drupal install for packer image
  hosts: all
  become: true
  vars:
    - target_app: nerdperk
  roles:
    - drupal_dependencies
    - drupal_deploy
    # Do NOT include drupal_db_ops here if it targets a specific DB server
    # Do NOT include drupal_storage if it mounts environment-specific NFS